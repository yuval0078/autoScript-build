Place ffmpeg.exe and ffprobe.exe here if you want to run from source without installing ffmpeg system-wide.

The build script build_pyinstaller.ps1 will try to locate ffmpeg/ffprobe on the build machine and copy them into this folder for packaging.
